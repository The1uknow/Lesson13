чтобы воспользоваться базами скл нужно импортировать(на маке встроен)
назвать переменную, передать параметр sqlite3.connect, открыть скобку, передать кавычки, указать название файла с расширением db. если файл н существует он создается автоматом
назвать новую переменную, в правом операнде обратиться к первой переменной и передать курсор cursor()

пример:
import sqlite3
q = sqlite3.connect("base1.db")
c = q.cursor()

commit() -для завершения и сохранения операций